---
title: "Pricing"
description: "How model pricing works on Nito and where to read per-model cost."
---

This page explains how Nito prices inference and where to read the numbers. The per-model rates are configured in the billing system and surfaced on each model's catalog entry, so the authoritative figures come from the live catalog, not a static table here.

## How Pricing Works

Nito bills in credits, tracked internally in nanos (billionths of a unit) for precision. Inference is priced primarily per token, with separate rates for input and output, plus additional rates for cached input tokens, reasoning output tokens, and server-side tool usage. Image generation is priced by output. Before each call, Nito places a hold sized to the expected cost, then settles it against actual usage. Failed requests are not billed. See [Check usage and credits](/get-started/check-usage-and-credits).

## What Gets Billed

A single call can accrue charges across several rate categories, each priced separately:

| Category | Priced by | Notes |
|---|---|---|
| Input tokens | Per token | Separate rate from output. |
| Output tokens | Per token | Separate rate from input. |
| Cached input tokens | Per token | Cache read and cache write are metered separately from ordinary input. |
| Reasoning output | Per token | Reasoning tokens are billed on top of ordinary output. |
| Server-side tools | Per use | Web search, web fetch, and similar hosted tools bill in addition to model tokens. |
| Image generation | By output | Images are priced by output, not by input tokens. |

Input and output are always separate rates; cache, reasoning, and server-tool charges are additive on top. Absence of a rate on a model entry means it is not yet configured, not that the item is free.

## Where to Read Per-Model Cost

Each model's `GET /v1/models` entry carries a `pricing` object (joined from the billing system at response time) alongside its `context_window`. Its per-token rates are expressed in nanos and cover input and output tokens, cache read and write, the reasoning output rate, web search and fetch, and an image output rate, plus `context_limit_tokens` for the model's context limit. The object also carries `currency_code` (defaults to `USD`), `unit` (`nanos`), and a price `version`; and each per-token rate is mirrored as a `*_million_token_nanos` field (the rate per million tokens) for convenience. Read pricing from the entry so your estimates track the live configuration.

```shellscript wrap
curl "https://nito.dev.zprotocol.org/v1/models" \
  -H "Authorization: Bearer $NITO_API_KEY"
# each entry includes a "pricing" object in nanos when configured
```

Rates are configured live and read from each model's `pricing` object, so there is no canonical static price table — but the [Model list](/models/all-models) page publishes a **snapshot** with each model's input/output price per 1M tokens for a quick overview. All amounts are in `nanos` (billionths of the currency in `currency_code`, default `USD`): divide by `1e9` for the value in dollars, so a `*_million_token_nanos` rate ÷ `1e9` gives the price per 1M tokens (e.g. `170000000` → `$0.17`).

## Server-Side Tool Pricing

Server-side tools such as web search and web fetch are billed in addition to the model tokens. Their per-use rates (`web_search_request_nanos`, `web_search_result_nanos`, `web_fetch_request_nanos`) are part of the same `pricing` object, read live. See [Tool pricing](/api-reference/server-tools/tool-pricing) for the reference.

## Estimating a Call

A rough estimate for a text call is `input_tokens * input_rate + output_tokens * output_rate`, plus any reasoning or tool costs, all in nanos. Because rates are per model and configured live, pull them from the `pricing` object rather than hard-coding numbers. The actual charge for a completed call is always reported in the response `usage` block and aggregated in the org usage overview.

## Common Issues

- **Hard-coding prices.** Rates are configured live and can change. Read them from the model entry.
- **Expecting `pricing` on every entry.** It appears only when configured for that model; absence means it is not yet set, not that the model is free.
- **Treating nanos as whole credits.** Nanos are billionths; divide by 1e9.
- **Forgetting tool and reasoning costs.** Web tools and reasoning tokens are billed on top of base input and output.

## Related Resources

<CardGroup cols={3}>
  <Card title="Model list" icon="list-timeline" href="/models/all-models">
    Snapshot of every model with per-1M prices.
  </Card>

  <Card title="Check usage and credits" icon="analytics" href="/get-started/check-usage-and-credits">
    Check consumption, credits, and balance.
  </Card>

  <Card title="Tool pricing" icon="toolbox" href="/api-reference/server-tools/tool-pricing">
    Server-side tool (web search / fetch) per-use rates.
  </Card>
</CardGroup>