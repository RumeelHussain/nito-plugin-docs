---
title: "Features"
description: "Everything you can do with one Nito key, one base URL, and one request. An honest map of what the gateway implements today."
---

Every feature here rides on the same frame: one key reaches the whole catalog, the `model` string selects both the model and how private the call is, and Nito handles routing, billing, and attestation underneath. If that frame is not yet familiar, read [How Nito Works](/get-started/how-nito-works) first; this page assumes it.

This is a map, not a tutorial. Each capability below links to its own deep page, covering what the gateway implements at this commit.

## Features Nito Offers

These are live on the gateway and documented in depth.

- [**Chat completions**](/features/chat-completions) the primary text surface. Send messages, name a model, get a reply. OpenAI-compatible shape, served at both `/v1/chat/completions` and `/api/v1/chat/completions`.
- [**Streaming**](/features/streaming) get the reply as Server-Sent Events token by token, by setting `stream: true`. Not available in Encrypted mode.
- [**Tool calling**](/features/tool-calling) declare function tools the model can ask to call. Your code runs them and returns the result; the gateway routes the conversation but does not execute your functions.
- [**Server-side tools**](/features/server-side-tools) tools Nito runs for you mid-generation: web search, web fetch, the file parser, and (xAI only) a code interpreter. You do not wire these up yourself.
- [**Reasoning**](/features/reasoning) ask a capable model to think first, with controls over effort and summary. Output arrives as `reasoning_details` plus a `reasoning_tokens` usage count.
- [**Multimodal**](/features/multimodal) image input and PDF/document input alongside text, plus text-to-image generation as a separate endpoint.
- [**Prompt caching**](/features/prompt-caching) pass provider cache directives (`prompt_cache_key`, `cache_control`) through to cut the cost of long, repeated prompt prefixes. Cache hits show up in `usage`.
- [**Safe retries**](/features/safe-retries) how to retry safely — failed calls are not billed, and how to guarantee exactly-once at your layer.

Nito also now serves an **Anthropic Messages-Compatible** endpoint at `POST /v1/messages` (and `/v1/messages/count_tokens`) and an **OpenAI Responses-Compatible** endpoint at `POST /v1/responses` (and `/v1/responses/compact`). Each validates its native request shape and maps it into the gateway's canonical chat contracts. You can try them in the [API Playground](/api-playground). Full drop-in guidance is documented in a later pass.

## Related Resources

<CardGroup cols={2}>
  <Card title="How Nito Works" icon="gears" href="/get-started/how-nito-works">
    One-key, many-models, selectable-privacy model.
  </Card>

  <Card title="Chat Completions" icon="message-dots" href="/features/chat-completions">
    Detailed explanation of the chat completions feature.
  </Card>

  <Card title="Privacy Overview" icon="lock-hashtag" href="/privacy/overview">
    Understand what “private” means on Nito.
  </Card>

  <Card title="Nito API Reference" icon="book-open-lines" href="/api-reference">
    Detailed reference of Nito API endpoints.<br />
  </Card>
</CardGroup>